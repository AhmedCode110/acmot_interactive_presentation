# Handoff prompt — paste everything below into a new Claude Code chat

---

You are continuing work on an **interactive HTML Master's presentation** for the project
**"AC-MOT: Adaptive Control for Real-Time Multi-Object Tracking"** (author: Ahmed Gouda Ismail,
Military Technical College, Cairo). The previous agent built it; your job is to **finish verification
and polish only** — do NOT rebuild from scratch and do NOT invent any numbers.

## Where everything is

Project folder: `~/Desktop/acmot_interactive_presentation/`

| File | Role |
|---|---|
| `index.html` | all **82 slides** (`<section class="slide">`), each with hidden speaker notes `<aside class="notes">` |
| `styles.css` | light academic theme; stage is 1600×900 scaled to screen |
| `script.js` | engine: navigation, fragments (`.frag`), notes (N), overview (O), fullscreen (F), tooltips (`data-tip`), click definitions (`data-def`), lightbox (`.zoomable`), tabs (`data-tabs`), SVG chart registry `CHARTS` (`data-chart="id"`) |
| `assets/data/results.js` | **single source of truth for every number** + provenance labels |
| `assets/data/cue_examples.js` | values generated by `tools/gen_figures.py` |
| `assets/figures/` | images/GIFs extracted from the user's deck `~/Desktop/DONT TOUCH/b9_claude.key` |
| `assets/figures/generated/` | edge / gray / blur example images |
| `assets/videos/` | 5 MP4s (all verified to decode in Chrome) |
| `README.md` | usage, controls, data provenance, how to update values |

The slide order mirrors the user's seminar deck `b9_claude.key` (sections I–VIII) plus the new
content: I Intro & metrics · II Related work · III Problem · IV AC-MOT framework · V Setup/protocols ·
VI Results (dev ablation, historical Baseline vs Full AC-MOT, TRK_MATCH_090, video) ·
VII Optimization (Optuna, V1 Trial 24, V1 test-dev, V2 Pareto, V2 Trial 22, 4-system dashboard,
bootstrap, UAVDT, cross-dataset table) · VIII U2MOT cross-pipeline (setup, fixes, speed, plan) ·
IX Contributions, is/is-not, limitations, future work, conclusion, summary, thank-you.

## How to preview and test

A static server config already exists in `/Users/ahmedgouda/CLAUDECODEX/claude/.claude/launch.json`
named **`acmot-presentation`** (python http.server on port 8765 serving the folder).
Start it with the Browser `preview_start` tool (name `acmot-presentation`), then open
`http://localhost:8765/index.html`. Jump to slide N from the console with
`document.querySelectorAll('.ov-grid button')[N-1].click()`; reveal all steps with
`document.querySelectorAll('.slide.active .frag').forEach(f=>f.classList.add('on'))`.
Wait ~2 s after jumping before a screenshot (charts animate). Local `file://` pages cannot be
scripted in the preview pane — always use the server.

Overflow audit used (returned **0 issues on all 82 slides** at last run): for each slide make it
visible, turn on all `.frag`, and flag any element inside `.s-body` whose bounding box leaves the
`.s-body` box by more than ~14 px.

## Status when handed over

DONE and verified in the browser (2026-09-14):
- 82 slides, 82 speaker notes, 35 asset references all present, 32 charts all defined, tags balanced
- no console errors; overflow audit clean on all 82 slides; all 5 videos decode (readyState 4)
- every slide screenshotted and checked at least once after the final fixes
- fixes applied: chart reference line (red "25" axis tick + legend caption), Pareto label layout,
  smoothing-chart label offsets, slide 61 left column overflow, slide 38 invalid SVG path,
  slide 71 code block wrap, "Old AC-MOT" label wrap, slides 8/9/14/32/33 spacing
- note: background videos (slides 45, 82) and slide 54 appear black/blank in the preview pane's
  screenshots only — the pane does not capture video frames; playback works in a real browser

REMAINING (optional only):
1. Cosmetic: a few slides (24, 26, 58, 77, 80) leave empty space at the bottom — could center
   content vertically. Not required.
2. When U2MOT official evaluation results exist: fill `u2mot` in assets/data/results.js and replace
   the word "pending" on slides 70 and 72 (see README → "How to update metric values later").
3. The preview server was stopped. To reopen for the user:
   `open ~/Desktop/acmot_interactive_presentation/index.html`

## Scientific rules you MUST keep (the user is strict about this)

- Never invent or change results. Every number lives in `assets/data/results.js`; sources are in
  `README.md` → "Where the results come from". Verified sources include
  `FINAL_TEST_RESULTS_3WORKER.json`, `FROZEN_DEFENSIBLE_ACMOT_CONFIG.json`,
  `docs/freeze/ACMOT_FINAL_SCIENTIFIC_FREEZE_2026-09-12.md` (git branch
  `freeze/final-after-uavdt-2026-09-12` of github.com/AhmedCode110/AC-MOT), repo `OFFICIAL_RESULTS`,
  and `acmot_full17_recorded_20260905_170655_summary.csv`.
- Every results slide keeps its provenance label (historical custom protocol / validation /
  held-out test-dev / external UAVDT / development ablation / published / pending / planned).
- All AC-MOT results use the **custom class-agnostic AC-MOT TrackEval protocol** — never call them
  official VisDrone leaderboard results; never compare them numerically with official-protocol or
  published numbers.
- Known corrections already applied — do not revert: ByteTrack 0.5/0.1/0.6/30/0.86 is NOT an archived
  config (verified profiles are on slide 44); V2 completed **49** of 50 trials; YOLO latency table is an
  *early exploratory measurement, raw log not archived*; U2MOT metrics stay **pending**;
  V1 does NOT have the lowest IDS (Old AC-MOT does on test-dev); V2 does not globally dominate V1;
  seminar charts `ch_gauges/ch_progress/ch_scatter/ch_weights` ("Trial 37") are simulated and must
  not be used.
- Simple English on slides; detail goes in speaker notes. Light theme only.
- Do not modify frozen evidence folders (`ACMOT_FROZEN_2026-09-11`, `ACMOT_V1_FINAL_POST_SNAPSHOT_2026-09-12`)
  or anything in `~/Desktop/DONT TOUCH/`.
